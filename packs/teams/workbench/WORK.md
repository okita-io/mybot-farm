# WORK.md — Workbench task board

The single source of truth for the Workbench team. Protocol in `TEAM.md` (same dir).
This distribution ships with the board reset — prior runs: see Shipped below.

## Board

(no active cards — add new cards here; the team works anything in `Ready`)

## Done

- Card 1 — Workbench status page: shipped by @workbench-smoke (after bounce #1 for
  Shipped-first ordering; re-fixed in ed3367b and all checks re-verified on the live
  board + repro fixture).
- Card 2 — Safeway hot sauce catalog: shipped by @workbench-smoke (all 4 accept checks
  pass on the running app; 27/27 cards render, filters AND-combined, data byte-identical
  to research snapshot + 6 live PDP re-verifications, dark theme / 375px / zero console
  errors / no CDN, bottle images + graceful fallback).

## Log

- 2026-09-13 — board reset for distribution; shipped history preserved above.
