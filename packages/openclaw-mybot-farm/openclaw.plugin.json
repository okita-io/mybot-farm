{
  "id": "mybot-farm",
  "name": "mybot.farm",
  "description": "Search mybot.farm stalls, plant GAF packs into OpenClaw, and post listings.",
  "version": "0.2.0",
  "categories": [
    "tools"
  ],
  "activation": {
    "onStartup": true
  },
  "configSchema": {
    "type": "object",
    "properties": {
      "baseUrl": {
        "type": "string",
        "default": "https://mybot.farm",
        "description": "mybot.farm API origin (env MYBOT_FARM_URL overrides)."
      },
      "workspaceRoot": {
        "type": "string",
        "default": "~/.openclaw/farm",
        "description": "Default parent dir for planted workspaces."
      },
      "apiKey": {
        "type": "string",
        "description": "Seller API key from https://mybot.farm/sell (prefer env MYBOT_FARM_API_KEY). Never commit the key."
      }
    },
    "additionalProperties": false
  },
  "uiHints": {
    "baseUrl": {
      "label": "Farm base URL",
      "help": "Defaults to https://mybot.farm. Env MYBOT_FARM_URL wins when set."
    },
    "workspaceRoot": {
      "label": "Workspace root",
      "help": "Planted agents land under this directory as ~/.openclaw/farm/<slug> by default."
    },
    "apiKey": {
      "label": "Seller API key",
      "help": "From https://mybot.farm/sell. Prefer env MYBOT_FARM_API_KEY. Never commit or log the key."
    }
  },
  "contracts": {
    "tools": [
      "farm_search",
      "farm_get_pack",
      "farm_plant",
      "farm_post",
      "farm_update"
    ]
  }
}
